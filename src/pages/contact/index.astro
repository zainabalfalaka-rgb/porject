---
import Layout from '../../layouts/Layout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';

const schemaData = {
  "@type": "ContactPage",
  "name": "اتصل بنا - دليل الخليج",
  "description": "تواصل مع فريق دليل الخليج للحصول على المساعدة والاستشارة في قضايا الاحتيال",
  "url": "https://khalijtrust.com/contact/"
};
---

<Layout 
  title="اتصل بنا - دليل الخليج"
  description="تواصل مع فريق دليل الخليج للحصول على المساعدة والاستشارة المجانية في قضايا الاحتيال واسترجاع الأموال"
  keywords="اتصل بنا، تواصل، استشارة مجانية، مساعدة، دعم، الخليج"
  schemaData={schemaData}
>
  <Header />
  
  <main>
    <!-- Page Header -->
    <section class="page-header">
      <div class="container">
        <div class="header-content">
          <h1 class="page-title">
            <span class="title-icon">📞</span>
            اتصل بنا
          </h1>
          <p class="page-description">
            نحن هنا لمساعدتك. تواصل معنا للحصول على استشارة مجانية أو للإبلاغ عن حالات احتيال 
            أو لأي استفسارات حول خدماتنا.
          </p>
        </div>
      </div>
    </section>

    <!-- Contact Methods -->
    <section class="contact-methods">
      <div class="container">
        <div class="methods-grid">
          <div class="method-card primary">
            <div class="method-icon">📱</div>
            <h3>واتساب</h3>
            <p>تواصل معنا مباشرة عبر واتساب للحصول على رد سريع</p>
            <div class="method-info">
              <span class="info-label">الرقم:</span>
              <span class="info-value">+971 4 510 9509</span>
            </div>
            <div class="method-hours">
              <span class="hours-label">ساعات العمل:</span>
              <span class="hours-value">24/7</span>
            </div>
            <a href="https://wa.me/97145109509" class="btn btn-primary" target="_blank">
              تواصل عبر واتساب
            </a>
          </div>
          
          <div class="method-card">
            <div class="method-icon">📧</div>
            <h3>البريد الإلكتروني</h3>
            <p>أرسل لنا رسالة مفصلة وسنرد عليك خلال 24 ساعة</p>
            <div class="method-info">
              <span class="info-label">البريد:</span>
              <span class="info-value">info@khalijtrust.com</span>
            </div>
            <div class="method-hours">
              <span class="hours-label">وقت الرد:</span>
              <span class="hours-value">خلال 24 ساعة</span>
            </div>
            <a href="mailto:info@khalijtrust.com" class="btn btn-outline">
              إرسال بريد إلكتروني
            </a>
          </div>
          
          <div class="method-card">
            <div class="method-icon">🏢</div>
            <h3>العنوان</h3>
            <p>يمكنك زيارتنا في مكتبنا الرئيسي في دبي</p>
            <div class="method-info">
              <span class="info-label">العنوان:</span>
              <span class="info-value">دبي، الإمارات العربية المتحدة</span>
            </div>
            <div class="method-hours">
              <span class="hours-label">ساعات العمل:</span>
              <span class="hours-value">الأحد - الخميس: 9:00 - 18:00</span>
            </div>
            <button class="btn btn-outline" onclick="alert('العنوان التفصيلي سيتم إرساله عند طلب الموعد')">
              طلب موعد
            </button>
          </div>
        </div>
      </div>
    </section>

    <!-- Contact Form -->
    <section class="contact-form-section">
      <div class="container">
        <div class="form-container">
          <div class="form-header">
            <h2>أرسل لنا رسالة</h2>
            <p>املأ النموذج أدناه وسنتواصل معك في أقرب وقت ممكن</p>
          </div>
          
          <form class="contact-form" id="contactForm">
            <div class="form-grid">
              <div class="form-group">
                <label for="firstName" class="form-label">الاسم الأول *</label>
                <input 
                  type="text" 
                  id="firstName" 
                  name="First Name" 
                  class="form-input" 
                  required
                  placeholder="أدخل اسمك الأول"
                />
              </div>
              
              <div class="form-group">
                <label for="lastName" class="form-label">اسم العائلة *</label>
                <input 
                  type="text" 
                  id="lastName" 
                  name="Last Name" 
                  class="form-input" 
                  required
                  placeholder="أدخل اسم العائلة"
                />
              </div>
              
              <div class="form-group">
                <label for="phone" class="form-label">رقم الهاتف *</label>
                <input 
                  type="tel" 
                  id="phone" 
                  name="Phone" 
                  class="form-input" 
                  required
                  placeholder="+971 50 123 4567"
                />
              </div>
              
              <div class="form-group">
                <label for="country" class="form-label">الدولة *</label>
                <select id="country" name="Country" class="form-select" required>
                  <option value="">اختر دولتك</option>
                  <option value="الإمارات العربية المتحدة">الإمارات العربية المتحدة</option>
                  <option value="المملكة العربية السعودية">المملكة العربية السعودية</option>
                  <option value="الكويت">الكويت</option>
                  <option value="قطر">قطر</option>
                  <option value="البحرين">البحرين</option>
                  <option value="عمان">عمان</option>
                  <option value="دولة أخرى">دولة أخرى</option>
                </select>
              </div>
              
              <div class="form-group">
                <label for="email" class="form-label">البريد الإلكتروني *</label>
                <input 
                  type="email" 
                  id="email" 
                  name="Email" 
                  class="form-input" 
                  required
                  placeholder="your@email.com"
                />
              </div>
              
              <div class="form-group">
                <label for="company" class="form-label">الشركة المتورطة</label>
                <input 
                  type="text" 
                  id="company" 
                  name="Company" 
                  class="form-input"
                  placeholder="اسم الشركة المشبوهة (إن وجدت)"
                />
              </div>
              
              <div class="form-group">
                <label for="money" class="form-label">المبلغ المتضرر (بالدرهم)</label>
                <input 
                  type="number" 
                  id="money" 
                  name="Money" 
                  class="form-input"
                  placeholder="0"
                  min="0"
                />
              </div>
              
              <div class="form-group">
                <label for="website" class="form-label">موقع الشركة الإلكتروني</label>
                <input 
                  type="url" 
                  id="website" 
                  name="Website" 
                  class="form-input"
                  placeholder="https://example.com"
                />
              </div>
              
              <div class="form-group">
                <label for="subject" class="form-label">الموضوع *</label>
                <select id="subject" name="Subject" class="form-select" required>
                  <option value="">اختر الموضوع</option>
                  <option value="consultation">استشارة قانونية</option>
                  <option value="report">الإبلاغ عن احتيال</option>
                  <option value="lawyer">البحث عن محامي</option>
                  <option value="complaint">شكوى</option>
                  <option value="partnership">شراكة</option>
                  <option value="other">أخرى</option>
                </select>
              </div>
            </div>
            
            <div class="form-group">
              <label for="message" class="form-label">الرسالة *</label>
              <textarea 
                id="message" 
                name="Description" 
                class="form-textarea" 
                rows="6" 
                required
                placeholder="اكتب رسالتك هنا... كلما كانت التفاصيل أكثر، كلما تمكنا من مساعدتك بشكل أفضل"
              ></textarea>
            </div>
            
            <div class="form-group">
              <div class="checkbox-group">
                <input type="checkbox" id="privacy" name="privacy" class="form-checkbox" required />
                <label for="privacy" class="checkbox-label">
                  أوافق على <a href="/privacy/">سياسة الخصوصية</a> وأفهم أن معلوماتي ستُستخدم للرد على استفساري
                </label>
              </div>
            </div>
            
            <div class="form-group">
              <div class="checkbox-group">
                <input type="checkbox" id="newsletter" name="newsletter" class="form-checkbox" />
                <label for="newsletter" class="checkbox-label">
                  أرغب في الحصول على تحديثات دورية حول التحذيرات الجديدة والنصائح القانونية
                </label>
              </div>
            </div>
            
            <button type="submit" class="btn btn-primary submit-btn">
              <span class="btn-text">إرسال الرسالة</span>
              <div class="loading-spinner" style="display: none;">
                <div class="spinner"></div>
              </div>
            </button>
          </form>
          
          <div class="form-message" id="formMessage" style="display: none;"></div>
        </div>
      </div>
    </section>

    <!-- FAQ Section -->
    <section class="faq-section">
      <div class="container">
        <div class="section-header">
          <h2>الأسئلة الشائعة</h2>
          <p>إجابات على أكثر الأسئلة شيوعاً</p>
        </div>
        
        <div class="faq-grid">
          <div class="faq-item">
            <div class="faq-question">
              <h3>هل الاستشارة مجانية؟</h3>
              <span class="faq-toggle">+</span>
            </div>
            <div class="faq-answer">
              <p>نعم، نقدم استشارة أولية مجانية لتقييم حالتك وتوجيهك للخطوات المناسبة. هذا يشمل تقييم قوة القضية وإمكانية استرجاع الأموال.</p>
            </div>
          </div>
          
          <div class="faq-item">
            <div class="faq-question">
              <h3>كم يستغرق الرد على الاستفسارات؟</h3>
              <span class="faq-toggle">+</span>
            </div>
            <div class="faq-answer">
              <p>نرد على الاستفسارات العاجلة عبر واتساب خلال ساعات قليلة، وعلى البريد الإلكتروني خلال 24 ساعة كحد أقصى.</p>
            </div>
          </div>
          
          <div class="faq-item">
            <div class="faq-question">
              <h3>هل تتعاملون مع قضايا من خارج الإمارات؟</h3>
              <span class="faq-toggle">+</span>
            </div>
            <div class="faq-answer">
              <p>نعم، نتعامل مع قضايا من جميع دول الخليج العربي ولدينا شبكة من المحامين المتخصصين في كل دولة.</p>
            </div>
          </div>
          
          <div class="faq-item">
            <div class="faq-question">
              <h3>ما هي المعلومات المطلوبة للاستشارة؟</h3>
              <span class="faq-toggle">+</span>
            </div>
            <div class="faq-answer">
              <p>نحتاج تفاصيل الحالة، المبلغ المتضرر، الوثائق المتاحة، ومعلومات الشركة أو الشخص المتسبب في الضرر.</p>
            </div>
          </div>
          
          <div class="faq-item">
            <div class="faq-question">
              <h3>هل معلوماتي آمنة؟</h3>
              <span class="faq-toggle">+</span>
            </div>
            <div class="faq-answer">
              <p>نعم، جميع المعلومات محمية بسرية تامة ولا نشاركها مع أي طرف ثالث إلا بموافقتك الصريحة أو للأغراض القانونية المطلوبة.</p>
            </div>
          </div>
          
          <div class="faq-item">
            <div class="faq-question">
              <h3>كيف يمكنني الإبلاغ عن شركة مشبوهة؟</h3>
              <span class="faq-toggle">+</span>
            </div>
            <div class="faq-answer">
              <p>يمكنك استخدام نموذج "بلغ عن احتيال" في موقعنا أو التواصل معنا مباشرة. سنقوم بالتحقق من المعلومات ونشر التحذير إذا ثبت صحتها.</p>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Emergency Contact -->
    <section class="emergency-section">
      <div class="container">
        <div class="emergency-card">
          <div class="emergency-icon">🚨</div>
          <div class="emergency-content">
            <h3>حالة طارئة؟</h3>
            <p>إذا كنت في حالة طارئة أو تعرضت لاحتيال كبير، تواصل معنا فوراً</p>
            <div class="emergency-actions">
              <a href="https://wa.me/97145109509?text=حالة طارئة - احتيال" class="btn btn-primary" target="_blank">
                واتساب طارئ
              </a>
              <a href="tel:+97145109509" class="btn btn-outline">
                اتصال مباشر
              </a>
            </div>
          </div>
        </div>
      </div>
    </section>
  </main>

  <Footer />
</Layout>

<script>
  document.addEventListener('DOMContentLoaded', function () {
    // Contact form handling
    const contactForm = document.getElementById('contactForm');
    const submitBtn = contactForm.querySelector('.submit-btn');
    const btnText = submitBtn.querySelector('.btn-text');
    const loadingSpinner = submitBtn.querySelector('.loading-spinner');
    const messageDiv = document.getElementById('formMessage');

    contactForm.addEventListener('submit', async function (e) {
      e.preventDefault();

      // Show loading state
      btnText.style.display = 'none';
      loadingSpinner.style.display = 'flex';
      submitBtn.disabled = true;

      const formData = new FormData(contactForm);
      const data = {
        firstName: formData.get('First Name'),
        lastName: formData.get('Last Name'),
        phone: formData.get('Phone'),
        country: formData.get('Country'),
        email: formData.get('Email'),
        company: formData.get('Company'),
        money: formData.get('Money'),
        website: formData.get('Website'),
        description: formData.get('Description'),
        privacy: formData.get('privacy'),
        newsletter: formData.get('newsletter'),
        timestamp: new Date().toISOString()
      };

      try {
        // Simulate successful form submission
        await new Promise(resolve => setTimeout(resolve, 1000));

        // Success
        messageDiv.textContent = 'تم إرسال رسالتك بنجاح! سنتواصل معك قريباً.';
        messageDiv.className = 'form-message success';
        messageDiv.style.display = 'block';

        // Reset form
        contactForm.reset();

        // Log data (in real implementation, send to server)
        console.log('Contact form submitted:', data);

      } catch (error) {
        // Error
        messageDiv.textContent = 'حدث خطأ أثناء إرسال الرسالة. يرجى المحاولة مرة أخرى أو التواصل معنا عبر واتساب.';
        messageDiv.className = 'form-message error';
        messageDiv.style.display = 'block';
      } finally {
        // Reset button state
        btnText.style.display = 'inline';
        loadingSpinner.style.display = 'none';
        submitBtn.disabled = false;

        // Hide message after 5 seconds
        setTimeout(() => {
          messageDiv.style.display = 'none';
        }, 5000);
      }
    });

    // FAQ toggle functionality
    const faqItems = document.querySelectorAll('.faq-item');
    faqItems.forEach(item => {
      const question = item.querySelector('.faq-question');
      const answer = item.querySelector('.faq-answer');
      const toggle = item.querySelector('.faq-toggle');

      question.addEventListener('click', async function () {
        const isOpen = item.classList.contains('open');

        // Simulate delay (if needed)
        await new Promise(resolve => setTimeout(resolve, 1000));

        // Toggle current item
        if (!isOpen) {
          item.classList.add('open');
          toggle.textContent = '−';
        } else {
          item.classList.remove('open');
          toggle.textContent = '+';
        }
      });
    });
  });
</script>

<style>
  .page-header {
    background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-primary-dark) 100%);
    color: white;
    padding: var(--space-16) 0;
    position: relative;
    overflow: hidden;
  }

  .page-header::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1000 1000"><polygon fill="rgba(255,255,255,0.05)" points="0,1000 1000,800 1000,1000"/></svg>');
    background-size: cover;
  }

  .header-content {
    text-align: center;
    position: relative;
    z-index: 1;
  }

  .page-title {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: var(--space-4);
    font-size: 3rem;
    margin-bottom: var(--space-6);
    color: white;
  }

  .title-icon {
    font-size: 3.5rem;
  }

  .page-description {
    font-size: 1.25rem;
    line-height: var(--line-height-relaxed);
    color: rgba(255, 255, 255, 0.9);
    max-width: 800px;
    margin: 0 auto;
  }

  .contact-methods {
    padding: var(--space-20) 0;
  }

  .methods-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
    gap: var(--space-8);
  }

  .method-card {
    background: white;
    padding: var(--space-8);
    border-radius: var(--radius-xl);
    box-shadow: var(--shadow-lg);
    text-align: center;
    transition: all 0.3s ease;
    border: 2px solid var(--color-neutral-100);
  }

  .method-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-xl);
  }

  .method-card.primary {
    border-color: var(--color-primary);
    background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-primary-light) 100%);
    color: white;
  }

  .method-card.primary:hover {
    border-color: var(--color-primary-dark);
  }

  .method-icon {
    font-size: 4rem;
    margin-bottom: var(--space-6);
  }

  .method-card h3 {
    font-size: 1.75rem;
    margin-bottom: var(--space-4);
    color: inherit;
  }

  .method-card p {
    font-size: 1.125rem;
    line-height: var(--line-height-relaxed);
    margin-bottom: var(--space-6);
    color: inherit;
    opacity: 0.9;
  }

  .method-info,
  .method-hours {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: var(--space-4);
    padding: var(--space-3) var(--space-4);
    background: rgba(255, 255, 255, 0.1);
    border-radius: var(--radius-md);
  }

  .method-card:not(.primary) .method-info,
  .method-card:not(.primary) .method-hours {
    background: var(--color-neutral-50);
  }

  .info-label,
  .hours-label {
    font-weight: var(--font-weight-semibold);
    font-size: 0.875rem;
  }

  .info-value,
  .hours-value {
    font-weight: var(--font-weight-bold);
    font-size: 0.875rem;
  }

  .method-card .btn {
    margin-top: var(--space-4);
    width: 100%;
  }

  .method-card.primary .btn-primary {
    background: white;
    color: var(--color-primary);
  }

  .method-card.primary .btn-primary:hover {
    background: var(--color-neutral-100);
  }

  .contact-form-section {
    background: var(--color-neutral-50);
    padding: var(--space-20) 0;
  }

  .form-container {
    max-width: 800px;
    margin: 0 auto;
    background: white;
    padding: var(--space-12);
    border-radius: var(--radius-xl);
    box-shadow: var(--shadow-lg);
  }

  .form-header {
    text-align: center;
    margin-bottom: var(--space-10);
  }

  .form-header h2 {
    font-size: 2.5rem;
    margin-bottom: var(--space-4);
    color: var(--color-neutral-900);
  }

  .form-header p {
    font-size: 1.125rem;
    color: var(--color-neutral-600);
    max-width: 600px;
    margin: 0 auto;
  }

  .contact-form {
    display: flex;
    flex-direction: column;
    gap: var(--space-6);
  }

  .form-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-6);
  }

  .form-group {
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
  }

  .form-label {
    font-weight: var(--font-weight-semibold);
    color: var(--color-neutral-700);
    font-size: 1rem;
  }

  .form-input,
  .form-select,
  .form-textarea {
    padding: var(--space-4);
    border: 2px solid var(--color-neutral-200);
    border-radius: var(--radius-md);
    font-size: 1rem;
    font-family: inherit;
    transition: border-color 0.2s ease;
    direction: rtl;
  }

  .form-input:focus,
  .form-select:focus,
  .form-textarea:focus {
    outline: none;
    border-color: var(--color-primary);
    box-shadow: 0 0 0 3px rgba(30, 64, 175, 0.1);
  }

  .form-textarea {
    resize: vertical;
    min-height: 120px;
    line-height: var(--line-height-relaxed);
  }

  .form-select {
    cursor: pointer;
  }

  .checkbox-group {
    display: flex;
    align-items: flex-start;
    gap: var(--space-3);
  }

  .form-checkbox {
    margin-top: var(--space-1);
    width: 20px;
    height: 20px;
    accent-color: var(--color-primary);
    cursor: pointer;
  }

  .checkbox-label {
    font-size: 0.9rem;
    color: var(--color-neutral-600);
    line-height: var(--line-height-relaxed);
    cursor: pointer;
  }

  .checkbox-label a {
    color: var(--color-primary);
    font-weight: var(--font-weight-semibold);
  }

  .submit-btn {
    align-self: center;
    display: flex;
    align-items: center;
    justify-content: center;
    min-width: 200px;
    height: 48px;
    position: relative;
    margin-top: var(--space-4);
  }

  .loading-spinner {
    display: none;
    align-items: center;
    justify-content: center;
  }

  .spinner {
    width: 20px;
    height: 20px;
    border: 2px solid rgba(255, 255, 255, 0.3);
    border-top: 2px solid white;
    border-radius: 50%;
    animation: spin 1s linear infinite;
  }

  @keyframes spin {
    0% { transform: rotate(0deg); }
    100% { transform: rotate(360deg); }
  }

  .form-message {
    padding: var(--space-4);
    border-radius: var(--radius-md);
    margin-top: var(--space-4);
    font-weight: var(--font-weight-semibold);
    text-align: center;
  }

  .form-message.success {
    background: rgba(16, 185, 129, 0.1);
    color: var(--color-success);
    border: 2px solid rgba(16, 185, 129, 0.2);
  }

  .form-message.error {
    background: rgba(239, 68, 68, 0.1);
    color: var(--color-error);
    border: 2px solid rgba(239, 68, 68, 0.2);
  }

  .faq-section {
    padding: var(--space-20) 0;
  }

  .section-header {
    text-align: center;
    margin-bottom: var(--space-12);
  }

  .section-header h2 {
    font-size: 2.5rem;
    margin-bottom: var(--space-4);
    color: var(--color-neutral-900);
  }

  .section-header p {
    font-size: 1.125rem;
    color: var(--color-neutral-600);
    max-width: 600px;
    margin: 0 auto;
  }

  .faq-grid {
    display: grid;
    gap: var(--space-4);
    max-width: 800px;
    margin: 0 auto;
  }

  .faq-item {
    background: white;
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-md);
    overflow: hidden;
    transition: all 0.3s ease;
  }

  .faq-question {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: var(--space-6);
    cursor: pointer;
    transition: background-color 0.2s ease;
  }

  .faq-question:hover {
    background: var(--color-neutral-50);
  }

  .faq-question h3 {
    font-size: 1.125rem;
    color: var(--color-neutral-900);
    margin: 0;
  }

  .faq-toggle {
    font-size: 1.5rem;
    font-weight: bold;
    color: var(--color-primary);
    transition: transform 0.3s ease;
  }

  .faq-item.open .faq-toggle {
    transform: rotate(45deg);
  }

  .faq-answer {
    max-height: 0;
    overflow: hidden;
    transition: max-height 0.3s ease;
  }

  .faq-item.open .faq-answer {
    max-height: 200px;
  }

  .faq-answer p {
    padding: 0 var(--space-6) var(--space-6);
    color: var(--color-neutral-600);
    line-height: var(--line-height-relaxed);
    margin: 0;
  }

  .emergency-section {
    background: var(--color-neutral-50);
    padding: var(--space-16) 0;
  }

  .emergency-card {
    background: linear-gradient(135deg, var(--color-error) 0%, #dc2626 100%);
    color: white;
    border-radius: var(--radius-xl);
    padding: var(--space-12);
    text-align: center;
  }

  .emergency-icon {
    font-size: 4rem;
    margin-bottom: var(--space-6);
  }

  .emergency-content h3 {
    font-size: 2rem;
    margin-bottom: var(--space-4);
    color: white;
  }

  .emergency-content p {
    font-size: 1.125rem;
    color: rgba(255, 255, 255, 0.9);
    margin-bottom: var(--space-8);
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
  }

  .emergency-actions {
    display: flex;
    gap: var(--space-4);
    justify-content: center;
    align-items: center;
  }

  .emergency-section .btn-outline {
    border-color: white;
    color: white;
  }

  .emergency-section .btn-outline:hover {
    background: white;
    color: var(--color-error);
  }

  @media (max-width: 768px) {
    .page-title {
      font-size: 2rem;
      flex-direction: column;
      gap: var(--space-2);
    }

    .title-icon {
      font-size: 3rem;
    }

    .methods-grid {
      grid-template-columns: 1fr;
      gap: var(--space-6);
    }

    .form-grid {
      grid-template-columns: 1fr;
      gap: var(--space-4);
    }

    .form-container {
      padding: var(--space-8);
    }

    .form-header h2 {
      font-size: 2rem;
    }

    .submit-btn {
      width: 100%;
    }

    .emergency-actions {
      flex-direction: column;
      gap: var(--space-3);
    }

    .emergency-actions .btn {
      width: 100%;
    }

    .section-header h2 {
      font-size: 2rem;
    }
  }
</style>