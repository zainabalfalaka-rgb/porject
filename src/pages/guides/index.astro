---
import Layout from '../../layouts/Layout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import CommentForm from '../../components/CommentForm.astro';

const schemaData = {
  "@type": "CollectionPage",
  "name": "أدلة توعوية - حماية من الاحتيال",
  "description": "أدلة شاملة لحمايتك من الاحتيال والنصب مع خطوات عملية وتوجيهات قانونية",
  "url": "https://khalijtrust.com/guides/"
};

// Sample guides data
const guides = [
  {
    id: 1,
    title: "كيف تتحقق من رخصة الشركة قبل الاستثمار",
    category: "التحقق من الشركات",
    readTime: "5 دقائق",
    difficulty: "سهل",
    description: "دليل شامل للتحقق من صحة رخصة الشركة وتجنب الشركات الوهمية",
    steps: 6,
    slug: "verify-company-license",
    featured: true,
    tags: ["رخصة", "تحقق", "استثمار"],
    lastUpdated: "2024-01-15"
  },
  {
    id: 2,
    title: "خطوات استرجاع الأموال من البنك",
    category: "الإجراءات المصرفية",
    readTime: "8 دقائق",
    difficulty: "متوسط",
    description: "الخطوات القانونية لاسترجاع أموالك من البنك في حالات الاحتيال",
    steps: 8,
    slug: "bank-refund-process",
    featured: true,
    tags: ["بنك", "استرجاع", "إجراءات"],
    lastUpdated: "2024-01-12"
  },
  {
    id: 3,
    title: "علامات التحذير من شركات الاستثمار الوهمية",
    category: "التعرف على الاحتيال",
    readTime: "6 دقائق",
    difficulty: "سهل",
    description: "تعلم كيف تكتشف علامات الإنذار المبكر للشركات المحتالة",
    steps: 10,
    slug: "investment-scam-warning-signs",
    featured: false,
    tags: ["استثمار", "علامات تحذير", "احتيال"],
    lastUpdated: "2024-01-10"
  },
  {
    id: 4,
    title: "كيف تحمي نفسك من احتيال العملات الرقمية",
    category: "العملات الرقمية",
    readTime: "10 دقائق",
    difficulty: "متقدم",
    description: "دليل شامل لحماية استثماراتك في العملات الرقمية من المحتالين",
    steps: 12,
    slug: "crypto-scam-protection",
    featured: true,
    tags: ["عملات رقمية", "حماية", "أمان"],
    lastUpdated: "2024-01-08"
  },
  {
    id: 5,
    title: "التعامل مع منصات التداول المشبوهة",
    category: "التداول",
    readTime: "7 دقائق",
    difficulty: "متوسط",
    description: "كيف تتعرف على منصات التداول الاحتيالية وتتجنبها",
    steps: 9,
    slug: "suspicious-trading-platforms",
    featured: false,
    tags: ["تداول", "منصات", "احتيال"],
    lastUpdated: "2024-01-05"
  }
];

const categories = [
  { value: "all", label: "جميع الفئات" },
  { value: "verification", label: "التحقق من الشركات" },
  { value: "banking", label: "الإجراءات المصرفية" },
  { value: "recognition", label: "التعرف على الاحتيال" },
  { value: "crypto", label: "العملات الرقمية" },
  { value: "trading", label: "التداول" }
];
---

<Layout 
  title="أدلة توعوية لحماية من الاحتيال - دليل الخليج"
  description="أدلة شاملة وخطوات عملية لحمايتك من الاحتيال والنصب مع توجيهات قانونية من خبراء في منطقة الخليج"
  keywords="أدلة توعوية، حماية من الاحتيال، خطوات عملية، توجيهات قانونية، تجنب النصب، الخليج"
  schemaData={schemaData}
>
  <Header />
  
  <main>
    <!-- Page Header -->
    <section class="page-header">
      <div class="container">
        <div class="header-content">
          <h1 class="page-title">
            <span class="title-icon">📚</span>
            أدلة توعوية
          </h1>
          <p class="page-description">
            تعلم كيف تحمي نفسك وأموالك من الاحتيال والنصب من خلال أدلة عملية مفصلة 
            وخطوات واضحة من خبراء قانونيين ومختصين في الأمان المالي.
          </p>
          
          <div class="guides-stats">
            <div class="stat-item">
              <span class="stat-number">{guides.length}</span>
              <span class="stat-label">دليل توعوي</span>
            </div>
            <div class="stat-item">
              <span class="stat-number">{guides.reduce((sum, g) => sum + g.steps, 0)}</span>
              <span class="stat-label">خطوة عملية</span>
            </div>
            <div class="stat-item">
              <span class="stat-number">95%</span>
              <span class="stat-label">معدل الحماية</span>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Filters Section -->
    <section class="filters-section">
      <div class="container">
        <div class="filters-container">
          <div class="filter-group">
            <label for="categoryFilter" class="filter-label">الفئة:</label>
            <select id="categoryFilter" class="filter-select">
              {categories.map(category => (
                <option value={category.value}>{category.label}</option>
              ))}
            </select>
          </div>
          
          <div class="filter-group">
            <label for="difficultyFilter" class="filter-label">المستوى:</label>
            <select id="difficultyFilter" class="filter-select">
              <option value="all">جميع المستويات</option>
              <option value="easy">سهل</option>
              <option value="medium">متوسط</option>
              <option value="advanced">متقدم</option>
            </select>
          </div>
          
          <div class="filter-group">
            <label for="sortFilter" class="filter-label">ترتيب حسب:</label>
            <select id="sortFilter" class="filter-select">
              <option value="recent">الأحدث</option>
              <option value="popular">الأكثر شعبية</option>
              <option value="steps">عدد الخطوات</option>
              <option value="time">وقت القراءة</option>
            </select>
          </div>
        </div>
      </div>
    </section>

    <!-- Featured Guides -->
    <section class="featured-guides">
      <div class="container">
        <div class="section-header">
          <h2>الأدلة المميزة</h2>
          <p>أهم الأدلة التي يجب على كل شخص معرفتها لحماية نفسه من الاحتيال</p>
        </div>
        
        <div class="featured-grid">
          {guides.filter(guide => guide.featured).map(guide => (
            <article class="guide-card featured">
              <div class="card-header">
                <div class="guide-meta">
                  <span class="category">{guide.category}</span>
                  <span class="featured-badge">مميز</span>
                </div>
                <div class="guide-stats">
                  <span class="read-time">⏱️ {guide.readTime}</span>
                  <span class="difficulty difficulty-{guide.difficulty.toLowerCase()}">{guide.difficulty}</span>
                </div>
              </div>
              
              <div class="card-body">
                <h3 class="guide-title">
                  <a href={`/guides/${guide.slug}/`}>{guide.title}</a>
                </h3>
                <p class="guide-description">{guide.description}</p>
                
                <div class="guide-info">
                  <div class="steps-count">
                    <span class="icon">📋</span>
                    <span>{guide.steps} خطوات</span>
                  </div>
                  <div class="last-updated">
                    <span class="icon">📅</span>
                    <span>آخر تحديث: {new Date(guide.lastUpdated).toLocaleDateString('ar-AE')}</span>
                  </div>
                </div>
                
                <div class="guide-tags">
                  {guide.tags.map(tag => (
                    <span class="tag">{tag}</span>
                  ))}
                </div>
              </div>
              
              <div class="card-footer">
                <a href={`/guides/${guide.slug}/`} class="btn btn-primary">
                  قراءة الدليل
                </a>
                <button class="btn btn-outline bookmark-btn" data-guide-id={guide.id}>
                  حفظ للمراجعة
                </button>
              </div>
            </article>
          ))}
        </div>
      </div>
    </section>

    <!-- All Guides -->
    <section class="all-guides">
      <div class="container">
        <div class="section-header">
          <h2>جميع الأدلة التوعوية</h2>
        </div>
        
        <div class="guides-grid">
          {guides.map(guide => (
            <article class="guide-card">
              <div class="card-header">
                <div class="guide-meta">
                  <span class="category">{guide.category}</span>
                  {guide.featured && <span class="featured-badge">مميز</span>}
                </div>
                <div class="guide-stats">
                  <span class="read-time">⏱️ {guide.readTime}</span>
                  <span class="difficulty difficulty-{guide.difficulty.toLowerCase()}">{guide.difficulty}</span>
                </div>
              </div>
              
              <div class="card-body">
                <h3 class="guide-title">
                  <a href={`/guides/${guide.slug}/`}>{guide.title}</a>
                </h3>
                <p class="guide-description">{guide.description}</p>
                
                <div class="guide-info">
                  <div class="steps-count">
                    <span class="icon">📋</span>
                    <span>{guide.steps} خطوات</span>
                  </div>
                  <div class="last-updated">
                    <span class="icon">📅</span>
                    <span>آخر تحديث: {new Date(guide.lastUpdated).toLocaleDateString('ar-AE')}</span>
                  </div>
                </div>
                
                <div class="guide-tags">
                  {guide.tags.map(tag => (
                    <span class="tag">{tag}</span>
                  ))}
                </div>
              </div>
              
              <div class="card-footer">
                <a href={`/guides/${guide.slug}/`} class="btn btn-primary btn-sm">
                  قراءة الدليل
                </a>
                <button class="btn btn-outline btn-sm bookmark-btn" data-guide-id={guide.id}>
                  حفظ
                </button>
              </div>
            </article>
          ))}
        </div>
      </div>
    </section>

    <!-- Help Section -->
    <section class="help-section">
      <div class="container">
        <div class="help-card">
          <div class="help-icon">💡</div>
          <div class="help-content">
            <h3>هل تحتاج مساعدة إضافية؟</h3>
            <p>إذا لم تجد الدليل الذي تبحث عنه أو تحتاج استشارة شخصية، تواصل معنا</p>
            <div class="help-actions">
              <a href="/contact/" class="btn btn-primary">
                اتصل بنا
              </a>
              <a href="https://wa.me/97145109509" class="btn btn-outline" target="_blank">
                واتساب مباشر
              </a>
            </div>
          </div>
        </div>
      </div>
    </section>
  </main>

  <Footer />
</Layout>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    // Filter functionality
    const categoryFilter = document.getElementById('categoryFilter');
    const difficultyFilter = document.getElementById('difficultyFilter');
    const sortFilter = document.getElementById('sortFilter');
    
    function applyFilters() {
      console.log('Applying filters:', {
        category: categoryFilter?.value,
        difficulty: difficultyFilter?.value,
        sort: sortFilter?.value
      });
    }
    
    [categoryFilter, difficultyFilter, sortFilter].forEach(filter => {
      filter?.addEventListener('change', applyFilters);
    });
    
    // Bookmark functionality
    const bookmarkButtons = document.querySelectorAll('.bookmark-btn');
    bookmarkButtons.forEach(button => {
      button.addEventListener('click', function() {
        const guideId = this.dataset.guideId;
        const isBookmarked = this.classList.contains('bookmarked');
        
        if (isBookmarked) {
          this.classList.remove('bookmarked');
          this.textContent = 'حفظ';
        } else {
          this.classList.add('bookmarked');
          this.textContent = 'محفوظ';
        }
        
        // Here you would typically save to localStorage or send to server
        console.log(`Guide ${guideId} ${isBookmarked ? 'removed from' : 'added to'} bookmarks`);
      });
    });
  });
</script>

<style>
  .page-header {
    background: linear-gradient(135deg, var(--color-success) 0%, #059669 100%);
    color: white;
    padding: var(--space-16) 0;
    position: relative;
    overflow: hidden;
  }

  .page-header::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1000 1000"><polygon fill="rgba(255,255,255,0.05)" points="0,1000 1000,800 1000,1000"/></svg>');
    background-size: cover;
  }

  .header-content {
    text-align: center;
    position: relative;
    z-index: 1;
  }

  .page-title {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: var(--space-4);
    font-size: 3rem;
    margin-bottom: var(--space-6);
    color: white;
  }

  .title-icon {
    font-size: 3.5rem;
  }

  .page-description {
    font-size: 1.25rem;
    line-height: var(--line-height-relaxed);
    color: rgba(255, 255, 255, 0.9);
    max-width: 800px;
    margin: 0 auto var(--space-8);
  }

  .guides-stats {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-6);
    max-width: 600px;
    margin: 0 auto;
  }

  .stat-item {
    text-align: center;
    padding: var(--space-4);
    background: rgba(255, 255, 255, 0.15);
    border-radius: var(--radius-lg);
    backdrop-filter: blur(10px);
  }

  .stat-number {
    display: block;
    font-size: 2rem;
    font-weight: var(--font-weight-bold);
    color: var(--color-secondary-light);
    margin-bottom: var(--space-1);
  }

  .stat-label {
    font-size: 0.875rem;
    color: rgba(255, 255, 255, 0.8);
  }

  .filters-section {
    background: var(--color-neutral-50);
    padding: var(--space-8) 0;
    border-bottom: 1px solid var(--color-neutral-200);
  }

  .filters-container {
    display: flex;
    align-items: center;
    gap: var(--space-8);
    flex-wrap: wrap;
  }

  .filter-group {
    display: flex;
    align-items: center;
    gap: var(--space-3);
  }

  .filter-label {
    font-weight: var(--font-weight-semibold);
    color: var(--color-neutral-700);
    white-space: nowrap;
  }

  .filter-select {
    padding: var(--space-2) var(--space-4);
    border: 2px solid var(--color-neutral-200);
    border-radius: var(--radius-md);
    background: white;
    font-family: inherit;
    font-size: 1rem;
    min-width: 180px;
    transition: border-color 0.2s ease;
  }

  .filter-select:focus {
    outline: none;
    border-color: var(--color-success);
  }

  .featured-guides {
    padding: var(--space-16) 0;
  }

  .section-header {
    text-align: center;
    margin-bottom: var(--space-12);
  }

  .section-header h2 {
    font-size: 2.5rem;
    margin-bottom: var(--space-4);
    color: var(--color-neutral-900);
  }

  .section-header p {
    font-size: 1.125rem;
    color: var(--color-neutral-600);
    max-width: 600px;
    margin: 0 auto;
  }

  .featured-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(400px, 1fr));
    gap: var(--space-8);
  }

  .all-guides {
    background: var(--color-neutral-50);
    padding: var(--space-16) 0;
  }

  .guides-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
    gap: var(--space-6);
  }

  .guide-card {
    background: white;
    border-radius: var(--radius-xl);
    box-shadow: var(--shadow-md);
    overflow: hidden;
    transition: all 0.3s ease;
    border: 2px solid var(--color-neutral-100);
  }

  .guide-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-xl);
    border-color: var(--color-success);
  }

  .guide-card.featured {
    border-color: var(--color-secondary);
    box-shadow: var(--shadow-lg);
  }

  .card-header {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    padding: var(--space-6);
    background: linear-gradient(135deg, var(--color-neutral-50) 0%, white 100%);
  }

  .guide-meta {
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
  }

  .category {
    background: var(--color-success);
    color: white;
    padding: var(--space-1) var(--space-3);
    border-radius: var(--radius-md);
    font-size: 0.75rem;
    font-weight: var(--font-weight-bold);
    text-transform: uppercase;
  }

  .featured-badge {
    background: var(--color-secondary);
    color: white;
    padding: var(--space-1) var(--space-3);
    border-radius: var(--radius-md);
    font-size: 0.75rem;
    font-weight: var(--font-weight-bold);
    text-transform: uppercase;
  }

  .guide-stats {
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
    align-items: flex-end;
  }

  .read-time {
    font-size: 0.875rem;
    color: var(--color-neutral-600);
  }

  .difficulty {
    padding: var(--space-1) var(--space-3);
    border-radius: var(--radius-md);
    font-size: 0.75rem;
    font-weight: var(--font-weight-bold);
    text-transform: uppercase;
  }

  .difficulty-سهل {
    background: var(--color-success);
    color: white;
  }

  .difficulty-متوسط {
    background: var(--color-secondary);
    color: white;
  }

  .difficulty-متقدم {
    background: var(--color-error);
    color: white;
  }

  .card-body {
    padding: var(--space-6);
  }

  .guide-title {
    font-size: 1.25rem;
    margin-bottom: var(--space-4);
  }

  .guide-title a {
    color: var(--color-neutral-900);
    text-decoration: none;
    transition: color 0.2s ease;
  }

  .guide-title a:hover {
    color: var(--color-success);
  }

  .guide-description {
    color: var(--color-neutral-600);
    line-height: var(--line-height-relaxed);
    margin-bottom: var(--space-6);
  }

  .guide-info {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-4);
    margin-bottom: var(--space-6);
    padding: var(--space-4);
    background: var(--color-neutral-50);
    border-radius: var(--radius-md);
  }

  .steps-count,
  .last-updated {
    display: flex;
    align-items: center;
    gap: var(--space-2);
    font-size: 0.875rem;
    color: var(--color-neutral-600);
  }

  .icon {
    font-size: 1rem;
  }

  .guide-tags {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-2);
    margin-bottom: var(--space-6);
  }

  .tag {
    background: var(--color-primary);
    color: white;
    padding: var(--space-1) var(--space-3);
    border-radius: var(--radius-md);
    font-size: 0.75rem;
    font-weight: var(--font-weight-semibold);
  }

  .card-footer {
    display: flex;
    gap: var(--space-3);
    padding: var(--space-6);
    background: var(--color-neutral-50);
    border-top: 1px solid var(--color-neutral-100);
  }

  .card-footer .btn {
    flex: 1;
    justify-content: center;
  }

  .btn-sm {
    padding: var(--space-2) var(--space-4);
    font-size: 0.875rem;
  }

  .bookmark-btn.bookmarked {
    background: var(--color-secondary);
    color: white;
    border-color: var(--color-secondary);
  }

  .help-section {
    padding: var(--space-16) 0;
  }

  .help-card {
    background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-primary-dark) 100%);
    color: white;
    border-radius: var(--radius-xl);
    padding: var(--space-12);
    text-align: center;
  }

  .help-icon {
    font-size: 4rem;
    margin-bottom: var(--space-6);
  }

  .help-content h3 {
    font-size: 2rem;
    margin-bottom: var(--space-4);
    color: white;
  }

  .help-content p {
    font-size: 1.125rem;
    color: rgba(255, 255, 255, 0.9);
    margin-bottom: var(--space-8);
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
  }

  .help-actions {
    display: flex;
    gap: var(--space-4);
    justify-content: center;
    align-items: center;
  }

  .help-section .btn-outline {
    border-color: white;
    color: white;
  }

  .help-section .btn-outline:hover {
    background: white;
    color: var(--color-primary);
  }

  @media (max-width: 768px) {
    .page-title {
      font-size: 2rem;
      flex-direction: column;
      gap: var(--space-2);
    }

    .title-icon {
      font-size: 3rem;
    }

    .guides-stats {
      grid-template-columns: 1fr;
      gap: var(--space-4);
    }

    .filters-container {
      flex-direction: column;
      align-items: stretch;
      gap: var(--space-4);
    }

    .filter-group {
      justify-content: space-between;
    }

    .filter-select {
      min-width: auto;
      flex: 1;
    }

    .featured-grid,
    .guides-grid {
      grid-template-columns: 1fr;
      gap: var(--space-4);
    }

    .guide-info {
      grid-template-columns: 1fr;
      gap: var(--space-3);
    }

    .card-footer {
      flex-direction: column;
      gap: var(--space-3);
    }

    .help-actions {
      flex-direction: column;
      gap: var(--space-3);
    }

    .help-actions .btn {
      width: 100%;
    }

    .section-header h2 {
      font-size: 2rem;
    }
  }
</style>